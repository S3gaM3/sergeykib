# 🚀 Инструкция по деплою на GitHub Pages

## Способ 1: Автоматический деплой через GitHub Actions (Рекомендуется)

Этот способ автоматически деплоит сайт при каждом push в ветку `main`.

### Настройка:

1. **Важно**: Убедитесь, что в репозитории включен GitHub Pages:
   - Перейдите в `Settings` → `Pages` вашего репозитория
   - В разделе `Source` выберите **`GitHub Actions`** (не Branch!)
   - Если опции нет, включите Pages в настройках репозитория

2. Файл `.github/workflows/deploy.yml` уже настроен. Просто сделайте push:
```bash
git add .
git commit -m "Setup Next.js deployment"
git push origin main
```

3. GitHub Actions автоматически:
   - Установит зависимости
   - Соберет проект
   - Задеплоит на GitHub Pages

4. Через несколько минут сайт будет доступен по адресу:
   - `https://sergeykib.github.io/` (если репозиторий `sergeykib.github.io`)
   - `https://sergeykib.github.io/sergeykib/` (если обычный репозиторий)

---

## Способ 2: Ручной деплой через скрипт

### Шаг 1: Сборка проекта

```bash
npm run build
```

Эта команда создаст папку `out/` со статическими файлами.

### Шаг 2: Экспорт в корень репозитория

```bash
npm run export:gh
```

Скрипт:
- Скопирует все файлы из `out/` в корень репозитория
- Оставит важные файлы (`.git`, `package.json`, исходники и т.д.)
- Удалит старые HTML/CSS/JS файлы (если есть)

### Шаг 3: Коммит и пуш

```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

### Шаг 4: Настройка GitHub Pages

1. Перейдите в `Settings` → `Pages` вашего репозитория
2. В разделе `Source` выберите:
   - **Branch**: `main`
   - **Folder**: `/ (root)`
3. Нажмите `Save`

---

## Способ 3: Деплой в ветку `gh-pages`

Если вы хотите хранить исходный код в `main`, а статические файлы в `gh-pages`:

### Шаг 1: Сборка и экспорт

```bash
npm run build
```

### Шаг 2: Переключение на ветку gh-pages

```bash
# Создаем ветку gh-pages (если её нет)
git checkout --orphan gh-pages

# Удаляем все файлы (кроме .git)
git rm -rf .

# Копируем содержимое out/
cp -r out/* .

# Коммитим
git add .
git commit -m "Deploy to GitHub Pages"

# Пушим
git push origin gh-pages

# Возвращаемся в main
git checkout main
```

### Шаг 3: Настройка GitHub Pages

1. `Settings` → `Pages`
2. `Source`: `gh-pages` branch
3. `Folder`: `/ (root)`

---

## 🔧 Настройка базового пути (если нужно)

Если ваш сайт доступен НЕ из корня (например, `https://username.github.io/repository-name/`), нужно добавить `basePath` в `next.config.js`:

```javascript
const nextConfig = {
  output: 'export',
  basePath: '/repository-name', // Замените на имя вашего репозитория
  // ... остальные настройки
}
```

**Важно**: Для репозитория `sergeykib.github.io` базовый путь НЕ нужен!

---

## ✅ Проверка деплоя

После деплоя проверьте:

1. **Главная страница**: `https://sergeykib.github.io/`
2. **Страница резюме**: `https://sergeykib.github.io/resume/`
3. **404 страница**: `https://sergeykib.github.io/nonexistent`
4. **Статические файлы**: проверьте, что изображения и шрифты загружаются
5. **Яндекс.Метрика**: проверьте, что счетчик работает

---

## 🐛 Решение проблем

### Ошибка "Permission denied" или "403 Forbidden"

**Проблема**: Старый workflow пытается использовать токен без прав.

**Решение**:
1. Убедитесь, что удалили старый файл `.github/workflows/gh-pages.yml`
2. Используйте новый workflow `.github/workflows/deploy.yml`
3. В `Settings` → `Pages` → `Source` выберите **`GitHub Actions`** (не Branch!)
4. Если проблема сохраняется, проверьте права в `Settings` → `Actions` → `General` → `Workflow permissions`: должен быть выбран `Read and write permissions`

### Изображения не загружаются

- Убедитесь, что папка `public/assets/` скопирована в `out/assets/`
- Проверьте пути в коде: они должны начинаться с `/assets/...`

### Стили не применяются

- Убедитесь, что `app/globals.css` подключен в `app/layout.tsx`
- Проверьте, что CSS файлы в папке `out/`

### 404 ошибка на подстраницах

- GitHub Pages не поддерживает серверный роутинг
- Все ссылки должны вести на `.html` файлы или использовать `trailingSlash: true` в `next.config.js` (уже настроено)
- Файл `.nojekyll` должен быть в папке `out/` (добавлен автоматически в workflow)

---

## 📝 Рекомендации

1. **Используйте GitHub Actions** (Способ 1) для автоматического деплоя
2. **Храните исходный код** в репозитории вместе с собранными файлами
3. **Добавьте `.nojekyll`** в корень для отключения Jekyll обработки:
   ```bash
   touch out/.nojekyll
   ```

---

## 🔗 Полезные ссылки

- [Next.js Static HTML Export](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions для Pages](https://github.com/actions/deploy-pages)

---

**Удачи с деплоем! 🚀**
