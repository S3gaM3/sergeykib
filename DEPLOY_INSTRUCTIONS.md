# 🚀 Инструкция по деплою

## Автоматический деплой через GitHub Actions

Проект настроен на автоматический деплой. При каждом push в ветку `main` происходит автоматическая сборка и деплой на GitHub Pages.

### Первоначальная настройка (один раз)

1. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в `Settings` → `Pages`
   - В разделе `Source` выберите `GitHub Actions`
   - Сохраните изменения

2. **Проверьте, что файл `.github/workflows/deploy.yml` существует**

3. **Проверьте файл `CNAME`** (если используете кастомный домен):
   - Убедитесь, что файл содержит ваш домен (например, `segak.ru`)

### Как работает автоматический деплой

1. При каждом push в ветку `main` автоматически запускается workflow
2. Workflow выполняет:
   - Установку зависимостей (`npm ci`)
   - Проверку кода линтером (`npm run lint`)
   - Сборку проекта (`npm run build`)
   - Копирование `CNAME` и создание `.nojekyll` в папку `out/`
   - Загрузку артефакта на GitHub Pages
   - Автоматический деплой

3. Сайт будет доступен через несколько минут после push

### Ручной запуск деплоя

Если нужно запустить деплой вручную (без push):

1. Перейдите в `Actions` в репозитории
2. Выберите workflow `Deploy to GitHub Pages`
3. Нажмите `Run workflow`
4. Выберите ветку `main`
5. Нажмите `Run workflow`

### Проверка статуса деплоя

1. Перейдите в `Actions` в репозитории
2. Найдите последний запуск workflow
3. Проверьте статус каждого шага
4. Если все шаги зеленые - деплой успешен!

### Устранение проблем

**Проблема: Workflow не запускается**
- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Проверьте, что вы пушите в ветку `main`
- Проверьте настройки репозитория: `Settings` → `Actions` → `General` → разрешения

**Проблема: Сборка падает с ошибками**
- Проверьте логи в `Actions`
- Убедитесь, что все зависимости установлены
- Проверьте, что нет ошибок TypeScript или ESLint

**Проблема: Сайт не обновляется**
- Подождите несколько минут (GitHub Pages может кешировать)
- Очистите кеш браузера
- Проверьте, что деплой действительно завершился успешно

### Кастомный домен

Если используете кастомный домен:

1. **Настройте DNS записи** у вашего провайдера домена:
   ```
   Тип: A
   Имя: @
   Значение: 185.199.108.153
   
   Тип: A
   Имя: @
   Значение: 185.199.109.153
   
   Тип: A
   Имя: @
   Значение: 185.199.110.153
   
   Тип: A
   Имя: @
   Значение: 185.199.111.153
   ```

2. **Проверьте файл `CNAME`** - он должен содержать ваш домен

3. **В настройках GitHub Pages** (`Settings` → `Pages`):
   - Убедитесь, что кастомный домен указан правильно
   - Включите опцию `Enforce HTTPS` (после настройки DNS)

### Что происходит при сборке

1. **Установка зависимостей** - `npm ci` (чистая установка)
2. **Линтинг** - проверка кода на ошибки
3. **Сборка** - `npm run build` создает папку `out/` со статическими файлами
4. **Копирование CNAME** - автоматически копируется в `out/`
5. **Создание .nojekyll** - для правильной работы на GitHub Pages
6. **Загрузка артефакта** - файлы из `out/` загружаются на GitHub Pages
7. **Деплой** - файлы публикуются на сайте

### Полезные команды для локальной проверки

```bash
# Установить зависимости
npm install

# Запустить линтер
npm run lint

# Собрать проект локально
npm run build

# Проверить содержимое папки out/
ls -la out/

# Проверить наличие CNAME
cat out/CNAME

# Проверить наличие .nojekyll
ls -la out/.nojekyll
```

