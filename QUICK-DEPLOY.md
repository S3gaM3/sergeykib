# ⚡ Быстрый деплой - Решение проблемы с правами доступа

## Проблема
Если вы получили ошибку `Permission denied` или `403 Forbidden`, значит используется старый workflow.

## ✅ Решение (3 шага)

### Шаг 1: Настройка GitHub Pages

1. Перейдите в **Settings** → **Pages** вашего репозитория
2. В разделе **Source** выберите **`GitHub Actions`** (ОБЯЗАТЕЛЬНО, не Branch!)
3. Сохраните изменения

### Шаг 2: Проверка прав доступа

1. Перейдите в **Settings** → **Actions** → **General**
2. Прокрутите до раздела **Workflow permissions**
3. Выберите **"Read and write permissions"**
4. Отметьте **"Allow GitHub Actions to create and approve pull requests"** (опционально)
5. Нажмите **Save**

### Шаг 3: Проверка workflow файла

Убедитесь, что у вас есть файл `.github/workflows/deploy.yml` и НЕТ файла `.github/workflows/gh-pages.yml` (старый файл).

Если старый файл есть - удалите его:
```bash
rm .github/workflows/gh-pages.yml
git rm .github/workflows/gh-pages.yml
git commit -m "Remove old workflow"
git push
```

## 🚀 После настройки

Просто сделайте push в ветку `main`:
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

GitHub Actions автоматически соберет и задеплоит проект!

---

## 📋 Чеклист проверки

- [ ] В `Settings` → `Pages` → `Source` выбрано `GitHub Actions`
- [ ] В `Settings` → `Actions` → `Workflow permissions` выбрано `Read and write`
- [ ] Файл `.github/workflows/deploy.yml` существует
- [ ] Старый файл `.github/workflows/gh-pages.yml` удален
- [ ] Сделан push в ветку `main`

---

## 🔍 Проверка статуса деплоя

1. Перейдите в **Actions** вкладку вашего репозитория
2. Найдите последний workflow run
3. Если он успешен - сайт должен быть доступен через несколько минут
4. Если есть ошибки - проверьте логи

---

**После выполнения этих шагов деплой должен работать автоматически! 🎉**
