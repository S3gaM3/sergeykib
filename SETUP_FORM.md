# 🔧 Настройка формы для работы на GitHub Pages

## Проблема
Форма не работает на GitHub Pages, так как статические сайты не могут выполнять серверный код.

## Решение: Serverless функция на Vercel

Используем Vercel для создания простого API endpoint, который будет отправлять сообщения в Telegram.

---

## ✅ Быстрая настройка (5 минут)

### Шаг 1: Создайте отдельный репозиторий для API

1. Создайте новый репозиторий на GitHub: `sergeykib-telegram-api`
2. Скопируйте папку `vercel-telegram-api/` в новый репозиторий:
   ```bash
   # Создайте новый репозиторий на GitHub, затем:
   git clone https://github.com/ваш-username/sergeykib-telegram-api.git
   cp -r vercel-telegram-api/* sergeykib-telegram-api/
   cd sergeykib-telegram-api
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

### Шаг 2: Задеплойте на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `sergeykib-telegram-api`
5. Vercel автоматически определит проект - просто нажмите **"Deploy"**

### Шаг 3: Настройте переменные окружения

После деплоя:
1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте две переменные:
   
   **TELEGRAM_BOT_TOKEN:**
   - Value: `8138334337:AAHux3K_OPgr4jpkyb3Tm5mMOjy2z3cyW_w`
   - Environment: Production, Preview, Development
   
   **TELEGRAM_CHAT_ID:**
   - Value: `873320985`
   - Environment: Production, Preview, Development

3. Нажмите **Save**

### Шаг 4: Получите URL и обновите форму

1. После деплоя Vercel покажет URL (например: `https://sergeykib-telegram-api.vercel.app`)
2. Ваш endpoint будет: `https://sergeykib-telegram-api.vercel.app/api/telegram-simple`
3. Обновите `app/components/ContactFormSimple.tsx`:
   
   Найдите строку 34:
   ```typescript
   const API_URL = process.env.NEXT_PUBLIC_TELEGRAM_API_URL || 
     'https://sergeykib-telegram.vercel.app/api/telegram-simple'
   ```
   
   Замените на ваш реальный URL:
   ```typescript
   const API_URL = 'https://sergeykib-telegram-api.vercel.app/api/telegram-simple'
   ```

### Шаг 5: Задеплойте обновленную форму

```bash
git add .
git commit -m "Update form to use Vercel API endpoint"
git push origin main
```

GitHub Actions автоматически задеплоит проект, и форма будет работать!

---

## 🧪 Тестирование

1. Откройте ваш сайт на GitHub Pages
2. Найдите форму в разделе контактов
3. Заполните и отправьте тестовое сообщение
4. Проверьте Telegram - сообщение должно прийти

---

## 📝 Альтернатива: Использовать текущий репозиторий

Если не хотите создавать отдельный репозиторий:

1. Задеплойте текущий репозиторий на Vercel
2. Vercel автоматически найдет `api/telegram-simple.js`
3. URL будет: `https://sergeykib.vercel.app/api/telegram-simple`
4. Обновите форму с этим URL

**Но учтите:** Vercel будет пытаться собрать весь Next.js проект, что может быть медленнее.

---

## ✅ Готово!

После выполнения этих шагов форма будет работать на GitHub Pages через Vercel serverless функцию.

