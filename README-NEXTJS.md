# Портфолио Сергея Кибальника - Next.js версия

Современный портфолио на **Next.js 15** с использованием App Router, TypeScript и Tailwind CSS.

## 🚀 Начало работы

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для продакшена

```bash
npm run build
```

### Запуск продакшен версии

```bash
npm start
```

### Экспорт статических файлов (для GitHub Pages)

```bash
npm run build
```

После сборки файлы будут в папке `out/`, которую можно задеплоить на GitHub Pages.

## 📁 Структура проекта

```
├── app/
│   ├── api/              # API routes
│   │   └── contact/      # Форма обратной связи
│   ├── components/       # React компоненты
│   │   ├── Header.tsx
│   │   ├── ProjectsGrid.tsx
│   │   └── Certificates.tsx
│   ├── data/            # Данные проектов
│   │   └── projects.ts
│   ├── resume/          # Страница резюме
│   │   └── page.tsx
│   ├── templates/        # Страница шаблонов
│   ├── layout.tsx        # Корневой layout
│   ├── page.tsx          # Главная страница
│   ├── globals.css       # Глобальные стили + Tailwind
│   ├── not-found.tsx     # 404 страница
│   └── sitemap.ts        # Генератор sitemap
├── public/               # Статические файлы
│   ├── assets/          # Изображения, шрифты и т.д.
│   ├── manifest.json    # PWA манифест
│   └── robots.txt       # Файл для поисковых роботов
├── scripts/              # Скрипты для деплоя
├── next.config.js       # Конфигурация Next.js
├── tailwind.config.ts   # Конфигурация Tailwind CSS
├── postcss.config.mjs   # Конфигурация PostCSS
├── tsconfig.json        # Конфигурация TypeScript
└── package.json         # Зависимости проекта
```

## ✨ Особенности

- **Next.js 15.1** с App Router (последняя версия)
- **React 19** (последняя версия)
- **TypeScript 5.7** для типобезопасности
- **Tailwind CSS 3.4** для стилизации
- **Server-Side Rendering (SSR)** для лучшего SEO
- **Статический экспорт** для GitHub Pages
- **Оптимизированные изображения**
- **Яндекс.Метрика** интегрирована
- **Structured Data (JSON-LD)** для SEO
- **Адаптивный дизайн**
- **PWA поддержка**

## 📦 Зависимости

### Основные
- `next`: ^15.1.0
- `react`: ^19.0.0
- `react-dom`: ^19.0.0

### Dev зависимости
- `typescript`: ^5.7.0
- `@types/node`: ^22.10.0
- `@types/react`: ^19.0.0
- `@types/react-dom`: ^19.0.0
- `tailwindcss`: ^3.4.17
- `postcss`: ^8.4.49
- `autoprefixer`: ^10.4.20
- `eslint`: ^9.18.0
- `eslint-config-next`: ^15.1.0

## 🔧 Конфигурация

### next.config.js

Проект настроен на статический экспорт (`output: 'export'`) для деплоя на GitHub Pages.

### SEO

Все SEO мета-теги настроены в файлах:
- `app/layout.tsx` - общие мета-теги
- `app/page.tsx` - мета-теги главной страницы
- `app/resume/page.tsx` - мета-теги страницы резюме

### Яндекс.Метрика

Яндекс.Метрика интегрирована в `app/layout.tsx` с ID `103955852`. Цели отслеживаются через клиентские скрипты.

## 🚢 Деплой на GitHub Pages

### Автоматический деплой через GitHub Actions

Проект настроен на автоматический деплой через GitHub Actions. При каждом push в ветку `main` происходит автоматическая сборка и деплой на GitHub Pages.

**Настройка (один раз):**

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Source` выберите `GitHub Actions`
3. Сохраните изменения

**После этого:**
- При каждом push в `main` автоматически запускается workflow
- Проект собирается и деплоится на GitHub Pages
- Сайт будет доступен по адресу: `https://sergeykib.github.io/sergeykib/` (или ваш кастомный домен)

### Ручной деплой

Если нужно задеплоить вручную:

```bash
# 1. Соберите проект
npm run build

# 2. Экспортируйте в корень (опционально, для старого метода)
npm run export:gh

# 3. Закоммитьте и запушьте изменения
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

### Кастомный домен

Если используете кастомный домен (например, `segak.ru`):
1. Убедитесь, что файл `CNAME` содержит ваш домен
2. Настройте DNS записи у вашего провайдера домена
3. GitHub Actions автоматически скопирует `CNAME` в папку `out/` при сборке

## 🎨 Стилизация

Проект использует **Tailwind CSS** для стилизации с сохранением кастомных CSS переменных и классов для обратной совместимости. Все стили находятся в `app/globals.css`.

### Tailwind CSS

- Настроен через `tailwind.config.ts`
- Интегрирован с PostCSS
- Поддерживает кастомные цвета и шрифты
- Готов к постепенному рефакторингу компонентов

## 📝 Миграция со старой версии

Проект был полностью очищен от:
- ❌ Angular проектов (`portfolio/`, `web/`)
- ❌ Старых HTML файлов
- ❌ Старых JS/CSS файлов
- ❌ Дублирующихся папок и файлов
- ❌ Неиспользуемых конфигураций

Новая версия использует:
- ✅ `app/` вместо HTML файлов
- ✅ `app/globals.css` + Tailwind CSS вместо `css/styles.css`
- ✅ `app/data/projects.ts` вместо `js/projects.js`
- ✅ React компоненты вместо inline JavaScript

## 🐛 Известные проблемы

- Изображения используют обычный `<img>` вместо Next.js `Image` компонента для совместимости со статическим экспортом
- Для динамических изображений из внешних источников настроены remotePatterns в `next.config.js`

## 📄 Лицензия

Все права защищены © {новый Date().getFullYear()} Сергей Кибальник

## 📧 Контакты

- Email: kibalnikserg@yandex.ru
- Telegram: @kosmosega
- GitHub: [S3gaM3](https://github.com/S3gaM3)
